import BuildMatrixClient from "./BuildMatrixClient";

export const metadata = {
  title: "Build Matrix & Compatibility Management — Alkota Studio",
  description: "Configure system option selectability, component constraints, and platform rules.",
};

export default function StudioBuildMatrixPage() {
  return <BuildMatrixClient />;
}
